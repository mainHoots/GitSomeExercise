This repo serves as an  exercise involving command-line usage of
git and GitHub CLI within a Bash terminal.

This repo was created with GitHub CLI command within the Bash terminal:
gh repo create GitSomeExercise --public

Current exercise:  Creating GitHub repo via command-line usage of GitHub CLI
within a Bash terminal.
 
# GitSomeExercise
